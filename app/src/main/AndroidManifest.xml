<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyCurrencyActivity"></activity>
        <activity android:name=".HistoryCurrencyActivity" />
        <activity android:name=".MainCurrencyActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChargingStation"/>
        <activity android:name=".FindChargeStationActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".ListActivityChargeStation"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".MainMenuChargeStation"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".EmptyChargeStationActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".DetailFragment"
            android:theme="@style/AppTheme.NoActionBar"/>

    </application>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>